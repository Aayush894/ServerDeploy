{
    "builds": [
      {
        "src": "api/main.py",
        "use": "@vercel/python",
        "config": {
          "zeroConfig": true
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/main.py"
      }
    ],
    "buildCommand": "bash .vercel/build.sh"
  }